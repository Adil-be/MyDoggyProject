{% extends 'base.html.twig' %}

{% block title %}Hello AdoptionOfferController!
{% endblock %}

{% block body %}
    <div class="my-3 text-center">
        <h1>Apply for adoption</h1>
    </div>

    <div x></div>

    <h2>Your informations</h2>
    {{form_start(form)}}
    <div>
        {{form_row(form.adoptant)}}</div>
    <h2>Dogs you want to adopte:</h2>
    {% form_theme form.dogs 'bootstrap_5_horizontal_layout.html.twig' %}
    {{form_row(form.dogs)}}
    <h2>Your message</h2>
    {{form_row(form.messages)}}
    <div class="text-center">
        <button type="submit" class="btn btn-primary">Confirm your demmand</button>
    </div>

    {{form_end(form)}}
{% endblock %}
